;; Miscellaneous personal keybindings
;; Check if command is assigned to binding: C-h w <command name>
;; Check if key sequence is bound: C-h k <key sequence>

(global-set-key (kbd "C-<pause>") 'comment-or-uncomment-region)
